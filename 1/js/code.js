(function () {
	window.truncateString  = function (str, length) {
		return str.substring(0, length);
	};

	/**
	 * @param {String} date "2006-05-04"
	 * @return {Array[Number]} [2006, 05, 04]
	 */
	// window.parseDate = function (date) {
	// 	// TODO...
	// };
})();
